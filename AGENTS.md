# Project Rules

- Always write clean, readable code.
- Add comments only when they clarify non-obvious behavior.
- Use TypeScript for frontend code.
- Use Python type hints for backend code.
- Write or update tests for meaningful behavior changes.
- Update `README.md` after every major change.
- Keep secrets out of Git. Use `.env` locally and GitHub repository secrets for CI or deployment.
